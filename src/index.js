/**
 * Wake-Sleep STT Module
 *
 * A modular, event-driven speech-to-text system with wake/sleep word detection.
 * Completely free and offline using Vosk.
 */

import WakeSleepSTT from './WakeSleepSTT.js';

export default WakeSleepSTT;
export { WakeSleepSTT };
